# ── RPC endpoints (required) ───────────────────────────────────────────────────
# The Ankr public endpoints are NOT used as a fallback — they require an API key.
# If these vars are unset, viem falls back to the chain's built-in public RPC
# (rpc.sepolia.org / cloudflare-eth.com), which may be rate-limited.
# Set your own for reliable access.
#
# Free options that don't require auth:
#   PublicNode:  https://ethereum-sepolia-rpc.publicnode.com
#                https://ethereum-rpc.publicnode.com
#
# Free options with API key (generous free tier):
#   Alchemy:     https://eth-sepolia.g.alchemy.com/v2/YOUR_KEY
#                https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY
#   Infura:      https://sepolia.infura.io/v3/YOUR_KEY
#                https://mainnet.infura.io/v3/YOUR_KEY

NEXT_PUBLIC_SEPOLIA_RPC_URL=
NEXT_PUBLIC_MAINNET_RPC_URL=

# ── WalletConnect ──────────────────────────────────────────────────────────────
# Required for WalletConnect wallet connections (mobile wallets via QR).
# Injected wallets (MetaMask, Coinbase Wallet) work without this.
# Get a free projectId at https://cloud.walletconnect.com

NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# ── Runway Achievements (Sepolia-only NFT) ──────────────────────────────────────
# The deployed RunwayAchievements contract address. Leave empty until deployed —
# the game runs fully without it, achievements just stay "not yet minted".
# Deploy with: npx hardhat run scripts/deploy.js --network sepolia

NEXT_PUBLIC_ACHIEVEMENT_CONTRACT_ADDRESS=

# Deployer key for the above — a funded Sepolia-only wallet. Never prefix this
# with NEXT_PUBLIC_; that prefix gets bundled into client-side JS and would
# leak the key to every visitor's browser. Server/CLI use only, for deploy.js.

RUNWAY_DEPLOYER_PRIVATE_KEY=

# Optional — a private Sepolia RPC for the deploy script specifically.
# Falls back to NEXT_PUBLIC_SEPOLIA_RPC_URL, then the public Sepolia RPC.

RUNWAY_SEPOLIA_RPC_URL=
